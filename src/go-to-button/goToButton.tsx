import React from 'react';

const GoToButton = (props: { spin: boolean }) => {

    return (
        <button className='border-0 bg-transparent'>

        </button>
    );
}

export default GoToButton;
